# Test E2E — Onboarding non-admin et blocage /cockpit\n\n## Objectif\nVérifier qu'un nouveau compte créé via invite-code (donc sans `cockpit_user`/`cockpit_admin`) :\n1. Est bien redirigé vers `/onboarding` après signup avec session immédiate\n2. Peut finaliser l'onboarding et arrive sur `/cockpit`\n3. Se voit refuser l'accès par `ProtectedCockpitRoute` avec le motif `NO_ROLE` (carte « Accès non autorisé »)\n4. `useCockpitAuth` retourne bien `hasCockpitAccess=false`\n\n## Approche retenue\nTest **Playwright E2E** (le projet a déjà `playwright.config.ts` + `playwright-fixture.ts` via `lovable-agent-playwright-config`). Dossier `e2e/` actuellement vide → première suite. Pas de Vitest unit (l'objectif est end-